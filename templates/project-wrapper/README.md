# Project Wrapper Template

Copy `scripts/fs25.ps1` and `scripts/fs25.py` into an FS25 map project, then edit `fs25.py` for that project's profile, slices, build outputs, and install path.

The installed skills and Claude commands call this wrapper.

