---
name: fs25-install
description: Install the FS25 preview zip into the configured mods folder.
---

# FS25 Install

Run `.\scripts\fs25.ps1 install` and report the copied path plus SHA256 result.

Do not tell the user to run the install command; the agent runs it. Only ask the user to open FS25 after install passes.
