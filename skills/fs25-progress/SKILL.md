---
name: fs25-progress
description: Show FS25 map build and QA gate status.
---

# FS25 Progress

Run `.\scripts\fs25.ps1 status` and summarize the gate table.

Do not present wrapper commands as user tasks. Report what was checked, which gates pass, and whether the next action belongs to the agent or the human tester.
