---
name: fs25-run
description: Run one named FS25 map slice.
---

# FS25 Run

Run `.\scripts\fs25.ps1 run <slice-name>`. If no slice is provided, run `.\scripts\fs25.ps1 list`.

Do not tell the user to run wrapper commands. Run the requested slice yourself, then report what ran and whether its gate passed.
