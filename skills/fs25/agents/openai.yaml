interface:
  display_name: "FS25 Command"
  short_description: "Dispatch FS25 map commands"
  default_prompt: "Use $fs25-next to recommend the next FS25 map command."
policy:
  allow_implicit_invocation: true
