Install the current FS25 preview zip into the configured mods folder.

Agent runs:

```powershell
.\scripts\fs25.ps1 install
```

Report the target path and SHA256 result. Do not ask the user to run the install command.
