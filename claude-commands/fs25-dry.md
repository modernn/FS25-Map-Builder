Print the commands for one FS25 map slice without running it.

Use the user's argument as the slice name:

```powershell
.\scripts\fs25.ps1 run $ARGUMENTS --dry-run
```

If no argument is provided, run:

```powershell
.\scripts\fs25.ps1 list
```

