Show FS25 map build status in this project.

Agent runs:

```powershell
.\scripts\fs25.ps1 status
```

Summarize gate status and whether the next action belongs to the agent or the human tester. Do not present PowerShell commands as user next steps.
