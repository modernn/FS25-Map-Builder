Run FS25 automated slices until failure or human verification.

Agent runs:

```powershell
.\scripts\fs25.ps1 start --all
```

Stop at the first failed gate or human test gate. Do not present PowerShell commands as user next steps.
